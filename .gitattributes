# Normalize line endings: repo stores LF, working copy checks out LF everywhere
# (extension build tooling and vitest snapshots are EOL-sensitive).
* text=auto eol=lf

# Windows scripts keep CRLF
*.bat text eol=crlf

# Binary assets
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
